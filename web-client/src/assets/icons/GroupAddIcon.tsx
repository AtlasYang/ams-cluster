import { MdOutlineGroupAdd } from "react-icons/md";

export default function GroupAddIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <MdOutlineGroupAdd size={size} color={color} />;
}
