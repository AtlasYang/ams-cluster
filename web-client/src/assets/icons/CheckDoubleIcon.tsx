import { FaCheckDouble } from "react-icons/fa";

export default function CheckDoubleIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <FaCheckDouble size={size} color={color} />;
}
